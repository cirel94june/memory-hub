app = "memory-hub"
primary_region = "nrt"  # 东京，离中国近

[build]

[env]
  PORT = "8888"

[http_service]
  internal_port = 8888
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

[mounts]
  source = "memory_data"
  destination = "/app/data"
